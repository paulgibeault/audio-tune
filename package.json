{
  "name": "audio-tune",
  "version": "0.0.4",
  "private": true,
  "description": "Soundboard, sound lab and sequencer for Paul's Arcade, built on the fleet's shipped sound packs.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node tools/verify-artifact.mjs && node --test 'tests/*.test.js'"
  },
  "type": "module"
}
